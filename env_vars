 POSTGRES_USERNAME=user
 POSTGRES_PASSWORD=password
 POSTGRES_HOST=postgres_server # Onlx valid for docker , Change into real host address in production
 POSTGRES_DB=postgres
 AWS_BUCKET=my-bucket
 AWS_REGION=us-east-1
 AWS_PROFILE=test
 JWT_SECRET=testing
 URL=http://localhost:8080
API_HOST=api